package eInheritance;

public class imultiple_Inheritance {

/*
	 In this Multi Level Inheritance means extending two class at a time "while doing this we get an error"
	 At a time we can extend only one class not more than one class
	 At a time two classes was not extended in one class, it can extend only one class
	
	 ClassA				ClassB
	   |				   |
	   |----> ClassC <-----|
*/
}
